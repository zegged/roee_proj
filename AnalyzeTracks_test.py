import AnalyzeTracks


def test_AnalyzeTracks():
    assert AnalyzeTracks(TracksMat, TracksVecMat, TracksP, TracksX, TracksMissMat, TracksDataBinMat, TracksDataMat,
                  MergedRng, MergedTeta, MergedYc, ping_number, Tping, sigmaTeta, Win_dlt, Dlt_th, MaxTracks, CurrentTargetInd) == , "Should be "


if __name__ == "__main__":
    test_AnalyzeTracks()
    print("Everything passed")
